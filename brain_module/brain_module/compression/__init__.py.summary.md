# File: `__init__.py`

## Purpose
This file is an initializer for a Python module that provides context compression functionality.

## Key Components
- It imports the `SentenceCompressor` class from the `sentence_compressor` module.
- Exports the `SentenceCompressor` class as part of the module's API via the `__all__` variable.

## Important Logic
The file does not contain any explicit logic; its purpose is to import and expose modules within this package.

## Dependencies
- `.sentence_compressor`: Module containing the `SentenceCompressor` class.

## Notes
This initializer is part of a module focused on context compression for query-relevant sentences from chunks.